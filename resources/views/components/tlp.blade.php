<ul>
    <!-- ru -->
    @if($ruTlp)
    <li>Surat pelaporan ke instansi pemerintah</li>
    @endif

    @if($ruTlp2)
    <li>{{$ruInput2}}</li>
    @endif

    <!-- ri -->
    @if($riTlp)
    <li>PIE / Approvable Letter</li>
    @endif
    
    <!-- st -->
    @if($stTlp)
    <li>Protokol uji stabilitas</li>
    @endif

    @if($stTlp2)
    <li>Laporan uji stabilitas</li>
    @endif
    
    <!-- me -->
    @if($meTlp)
    <li>Daftar daluwarsa produk</li>
    @endif

    @if($meTlp2)
    <li>Screenshoot revisi daluarsa pada ERP</li>
    @endif
    
    <!-- val -->
    @if($valTlp)
    <li>Protokol validasi pengolahan</li>
    @endif
    
    @if($valTlp2)
    <li>Laporan validasi pengolahan</li>
    @endif

    @if($valTlp3)
    <li>Protokol validasi pengemasan</li>
    @endif

    @if($valTlp4)
    <li>Laporan validasi pengemasan</li>
    @endif

    @if($valTlp5)
    <li>Protokol validasi pembersihan</li>
    @endif

    @if($valTlp6)
    <li>Laporan validasi pembersihan</li>
    @endif

    @if($valTlp7)
    <li>Protokol validasi metode analisa</li>
    @endif

    @if($valTlp8)
    <li>Laporan validasi metode analisa</li>
    @endif

    @if($valTlp9)
    <li>Protokol kualifikasi instalasi</li>
    @endif

    @if($valTlp10)
    <li>Laporan kualifikasi instalasi</li>
    @endif

    @if($valTlp11)
    <li>Protokol kualifikasi operasional</li>
    @endif

    @if($valTlp12)
    <li>Laporan kualifikasi operasional</li>
    @endif

    @if($valTlp13)
    <li>Protokol kualifikasi kinerja</li>
    @endif

    @if($valTlp14)
    <li>Laporan kualifikasi kinerja</li>
    @endif
    
    <!-- tr -->
    @if($trTlp)
    <li>LHT skala laboratorium</li>
    @endif
    
    @if($trTlp2)
    <li>CPB skala pilot</li>
    @endif
    
    @if($trTlp3)
    <li>LHT skala pilot</li>
    @endif
    
    @if($trTlp4)
    <li>CPB Pengolahan</li>
    @endif
    
    @if($trTlp5)
    <li>Catatan analisa produk ruahan</li>
    @endif
    
    @if($trTlp6)
    <li>CPB Pengemasan</li>
    @endif
    
    <!-- pro -->
    @if($proTlp)
    <li>Laporan hasil uji</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($proTlp2)
    <li>Laporan hasil uji</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif

    @if($proTlp3)
    <li>Laporan hasil uji</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    <!-- dok -->
    @if($dokTlp)
    <li>Manual Mutu</li>
    @endif
    
    @if($dokTlp2)
    <li>Dokumen induk industri farmasi</li>
    @endif

    @if($dokTlp3)
    <li>Prosedur</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp4)
    <li>Ketentuan umum</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp5)
    <li>Protap</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp6)
    <li>Formulir</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp7)
    <li>Daftar</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp8)
    <li>Spesifikasi</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp9)
    <li>Metode analisa</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    @if($dokTlp10)
    <li>Metode analisa</li>
    <!-- ini dia kan ada input nah itu mau ditampilin gak? -->
    @endif
    
    <!-- dk -->
    @if($dkTlp)
    <li>Artwork Desain Kemasan</li>
    @endif

    <!-- sis -->
    @if($sisTlp)
    <li>Screenshoot perubahan pada ERP</li>
    @endif
</ul>